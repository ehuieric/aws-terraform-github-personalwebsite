name: Test

on
  push:
    branches:
      - master
  workflow_dispatch:

Jobs:
  logs_message:
    runs-on: ubuntu-latest
    steps:
      - name: log
        run: echo "The workflow is working!"    